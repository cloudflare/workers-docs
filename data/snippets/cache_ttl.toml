name = "Cache TTL"
description = "Determine how/if Cloudflare's [Classic CDN](/reference/workers-concepts/using-cache.md#cloudflare-s-cdn) and Browsers will cache a resource by setting TTL and cache settings."
code = """async function handleRequest(request) {
  let response = await fetch(request, {
    cf: {
      // Tell Cloudflare to always cache this fetch regardless of content type
      // for a max of 5 seconds before revalidating the resource
      cacheTtl: 5, // sets TTL to 5 and cacheEverything setting
    },
  })
  response = new Response(response.body, response)
  //Set cache control headers to cache on browser for 25 minutes
  response.headers.set('Cache-Control', 'max-age=1500')
  return response
}
addEventListener('fetch', event => {
  return event.respondWith(handleRequest(event.request))
})
"""
demo_url = "https://cloudflareworkers.com/#8dd3e29985701b05e43b4dd74c387b74:https://example.com/"
