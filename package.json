{
  "name": "cloudflare-workers-docs",
  "version": "1.0.0",
  "description": "Cloudflare Workers developer docs",
  "license": "Apache-2.0 OR MIT",
  "main": "workers-site/index.js",
  "private": true,
  "scripts": {
    "build": "hugo && wrangler build",
    "start-hugo": "hugo -D server",
    "start-worker": "hugo && wrangler preview --watch -e staging",
    "publish-staging": "npm run build && wrangler publish -e staging",
    "publish-production": "npm run build && wrangler publish -e production"
  },
  "dependencies": {
    "gulp": "^4.0.0",
    "@cloudflare/hugo-cloudflare-docs": "1.0.2"
  }
}
